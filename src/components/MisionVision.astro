<section class="mision-vision">
    <div class="section-header">
        <span class="accent-line"></span>
        <h2>Este Proyecto está diseñado para guiar a los futuros profesionales tecnológicos</h2>
        <span class="accent-line"></span>
    </div>
    
    <div class="cards-container">
        <article class="card mision-card">
            <div class="card-icon">
                <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                    <circle cx="12" cy="12" r="10"/>
                    <path d="M12 16v-4"/>
                    <path d="M12 8h.01"/>
                    <path d="M8 12h8"/>
                </svg>
            </div>
            <h3>Nuestra Misión</h3>
            <div class="card-image">
                <img src="/tecno-student.jpg" alt="Tecno Student">
                <div class="image-overlay"></div>
            </div>
            <p>"Simplificar el camino de aprendizaje para los futuros profesionales tecnológicos. En un mar de tutoriales y recursos infinitos, Skiltree nace para curar, organizar y estructurar el mejor contenido gratuito en español. Nuestra misión es que nadie deje de aprender programación o diseño por no saber por dónde empezar."</p>
        </article>
        
        <article class="card vision-card">
            <div class="card-icon">
                <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M2 12s3-7 10-7 10 7 10 7-3 7-10 7-10-7-10-7Z"/>
                    <circle cx="12" cy="12" r="3"/>
                </svg>
            </div>
            <h3>Nuestra Visión</h3>
            <div class="card-image">
                <img src="/tecno-map.jpg" alt="Tecno Map">
                <div class="image-overlay"></div>
            </div>
            <p>"Convertirnos en el mapa de referencia para la comunidad tech hispanohablante. Aspiramos a ser la herramienta donde cualquier persona, sin importar su nivel económico o conocimientos previos, pueda trazar su propia ruta de aprendizaje y transformar su carrera profesional de manera guiada y eficiente."</p>
        </article>
    </div>
</section>

<style>
    .mision-vision {
        max-width: 1200px;
        margin: 60px auto;
        padding: 0 20px;
    }

    .section-header {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 20px;
        margin-bottom: 50px;
        text-align: center;
    }

    .section-header h2 {
        font-size: clamp(1.4rem, 3vw, 2rem);
        color: #ffffff;
        max-width: 600px;
        line-height: 1.4;
        background: linear-gradient(135deg, #ffffff 0%, #00d4ff 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .accent-line {
        flex: 1;
        max-width: 100px;
        height: 2px;
        background: linear-gradient(90deg, transparent, #00d4ff, transparent);
    }

    .cards-container {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
        gap: 30px;
    }

    .card {
        background: linear-gradient(145deg, rgba(42, 42, 42, 0.9) 0%, rgba(26, 26, 26, 0.95) 100%);
        border-radius: 16px;
        padding: 30px;
        border: 1px solid rgba(0, 212, 255, 0.15);
        backdrop-filter: blur(10px);
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
        overflow: hidden;
    }

    .card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 3px;
        background: linear-gradient(90deg, #00d4ff, #00ff88, #00d4ff);
        background-size: 200% 100%;
        animation: shimmer 3s ease-in-out infinite;
    }

    @keyframes shimmer {
        0%, 100% { background-position: 200% 0; }
        50% { background-position: -200% 0; }
    }

    .card:hover {
        transform: translateY(-8px);
        border-color: rgba(0, 212, 255, 0.4);
        box-shadow: 
            0 20px 40px rgba(0, 0, 0, 0.4),
            0 0 30px rgba(0, 212, 255, 0.1);
    }

    .card-icon {
        width: 80px;
        height: 80px;
        margin: 0 auto 20px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, rgba(0, 212, 255, 0.1) 0%, rgba(0, 255, 136, 0.05) 100%);
        border-radius: 50%;
        border: 1px solid rgba(0, 212, 255, 0.2);
    }

    .card-icon svg {
        color: #00d4ff;
        transition: transform 0.3s ease;
    }

    .card:hover .card-icon svg {
        transform: scale(1.1);
    }

    .card h3 {
        font-size: 1.5rem;
        color: #00d4ff;
        text-align: center;
        margin-bottom: 20px;
        text-shadow: 0 0 20px rgba(0, 212, 255, 0.3);
    }

    .card-image {
        position: relative;
        border-radius: 12px;
        overflow: hidden;
        margin-bottom: 20px;
    }

    .card-image img {
        width: 100%;
        height: 200px;
        object-fit: cover;
        display: block;
        transition: transform 0.5s ease;
    }

    .card:hover .card-image img {
        transform: scale(1.05);
    }

    .image-overlay {
        position: absolute;
        inset: 0;
        background: linear-gradient(
            180deg,
            transparent 0%,
            rgba(0, 212, 255, 0.1) 50%,
            rgba(26, 26, 26, 0.8) 100%
        );
        pointer-events: none;
    }

    .card p {
        color: #b0b0b0;
        line-height: 1.7;
        font-size: 0.95rem;
        text-align: justify;
        font-style: italic;
    }

    .mision-card {
        --accent: #00d4ff;
    }

    .vision-card {
        --accent: #00ff88;
    }

    .vision-card::before {
        background: linear-gradient(90deg, #00ff88, #00d4ff, #00ff88);
        background-size: 200% 100%;
    }

    .vision-card .card-icon svg {
        color: #00ff88;
    }

    .vision-card h3 {
        color: #00ff88;
        text-shadow: 0 0 20px rgba(0, 255, 136, 0.3);
    }

    .vision-card .card-icon {
        background: linear-gradient(135deg, rgba(0, 255, 136, 0.1) 0%, rgba(0, 212, 255, 0.05) 100%);
        border-color: rgba(0, 255, 136, 0.2);
    }

    /* Responsive */
    @media (max-width: 768px) {
        .mision-vision {
            margin: 40px auto;
        }

        .section-header {
            flex-direction: column;
            gap: 15px;
        }

        .accent-line {
            width: 60px;
            max-width: none;
        }

        .cards-container {
            grid-template-columns: 1fr;
        }

        .card {
            padding: 25px;
        }

        .card-image img {
            height: 180px;
        }
    }
</style>