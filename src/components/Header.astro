---
interface Props {
  title?: string;
  subtitle?: string;
}

const { 
  title = "📚 SkillTree ", 
  subtitle = "Recursos de aprendizaje gratuitos organizados jerárquicamente" 
} = Astro.props;
---

<header>
  <h1><span class="material-symbols-outlined icon">account_tree</span> <span class="skill">Skill</span><span class="tree">Tree</span></h1>
  <p class="subtitle">{subtitle}</p>
</header>

<style>
  header {
    text-align: center;
    margin-bottom: 30px;
    padding: 20px;
    background: linear-gradient(135deg, #2a2a2a 0%, #1a1a1a 100%);
    border-radius: 8px;
    border: 1px solid #333;
  }

  header h1 {
    font-size: 2.5em;
    margin-bottom: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.3em;
  }

  .icon {
    font-size: 1em;
    color: #00d4ff;
    vertical-align: middle;
  }

  .skill {
    color: #ffffff;
    text-shadow: 0 0 10px rgba(255, 255, 255, 0.2);
  }

  .tree {
    color: #00d4ff;
    text-shadow: 0 0 10px rgba(0, 212, 255, 0.3);
  }

  .subtitle {
    color: #d2d2d2;
    font-size: 1.1em;
    font-weight: 500;
  }

  @media (max-width: 768px) {
    header h1 {
      font-size: 1.8em;
    }

    .subtitle {
      font-size: 0.9em;
    }
  }
</style>
